apiVersion: v1
kind: Service
metadata:
  name: govrix-ai-oss
  namespace: govrix
spec:
  selector:
    app: govrix-ai-oss
  ports:
  - name: proxy
    port: 4000
    targetPort: 4000
  - name: api
    port: 4001
    targetPort: 4001
